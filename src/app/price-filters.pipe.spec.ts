import { PriceFiltersPipe } from './price-filters.pipe';

describe('PriceFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
