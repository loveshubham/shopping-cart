import { SizeFiltersPipe } from './size-filters.pipe';

describe('SizeFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
